[Unit]
Description=trigger build on circleci for docker-aws
Requires=docker.service
After=docker.service

[Service]
Type=simple
Environment=CIRCLE_TOKEN=redacted
Environment=SNITCH_URL=https://nosnch.in/redacted
ExecStart=-/opt/sbin/trigger

[Install]
WantedBy=system-config.target
